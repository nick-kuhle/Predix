'use client';
import {useEffect,useMemo,useState} from 'react';
const assets=[['BTC','Bitcoin'],['ETH','Ethereum'],['SPY','S&P 500'],['GLD','Gold'],['EURUSD=X','Euro / Dollar'],['NVDA','NVIDIA']];const horizons=[['5m',1],['15m',3],['1h',12],['4h',48],['24h',288],['7d',2016],['30d',8640],['365d',105120]];
function Chart({prices,horizon,forecast}){let vals=prices.map(x=>x.value),last=vals.at(-1)||0,rate={"5m":.0007,"15m":.0015,"1h":.004,"4h":.009,"24h":.018,"7d":.037,"30d":.082,"365d":.24}[horizon]||.1;let projected=forecast?.point?.length?forecast.point.slice(0,12):Array.from({length:12},(_,i)=>last*(1+rate*(i+1)/12)+Math.sin(i)*last*rate*.13),all=vals.concat(projected),min=Math.min(...all),max=Math.max(...all),range=max-min||1,pts=vals.map((v,i)=>`${(i/(vals.length-1))*1000},${250-((v-min)/range)*210}`).join(' '),future=projected.map((v,i)=>`${1000+i*14},${250-((v-min)/range)*210}`).join(' ');return <svg viewBox="0 0 1160 270" preserveAspectRatio="none"><defs><linearGradient id="fill" x2="0" y2="1"><stop stopColor="#60e6df" stopOpacity=".3"/><stop offset="1" stopColor="#60e6df" stopOpacity="0"/></linearGradient></defs><g stroke="#ffffff12"><path d="M0 40H1160M0 100H1160M0 160H1160M0 220H1160"/></g><polyline points={`${pts} 1000,270 0,270`} fill="url(#fill)"/><polyline points={pts} fill="none" stroke="#60e6df" strokeWidth="3"/><line x1="1000" x2="1000" y1="10" y2="270" stroke="#ffffff55" strokeDasharray="4 6"/><polyline points={future} fill="none" stroke="#a395ff" strokeWidth="3" strokeDasharray="8 7"/><circle cx="1168" cy={future.split(' ').at(-1)?.split(',')[1]} r="6" fill="#a395ff"/></svg>}
export default function Home(){const [symbol,setSymbol]=useState('BTC'),[horizon,setHorizon]=useState('7d'),[data,setData]=useState(null),[loading,setLoading]=useState(true),[showSummary,setShowSummary]=useState(true),[notice,setNotice]=useState(''),[apiKey,setApiKey]=useState(''),[showKeyPrompt,setShowKeyPrompt]=useState(true);const load=()=>{setLoading(true);fetch('/api/market?symbol='+encodeURIComponent(symbol),{headers:apiKey?{'x-tsfm-api-key':apiKey}:{}}).then(async r=>{const j=await r.json();if(!r.ok)throw Error(j.error||'Data unavailable');setData(j);setNotice(j.forecast?.engine?'Google TimesFM connected · live forecast':(j.stale?'Using cached market data':'Market data loaded. Add a valid TSFM key to enable TimesFM.'))}).catch(()=>setNotice('Live provider unavailable — showing the last successful data.')).finally(()=>setLoading(false))};useEffect(()=>{load()},[symbol]);let last=data?.prices?.at(-1)?.value||0;let rate=(horizons.find(x=>x[0]===horizon)?.[1]||1)/10000;let expected=last*(1+rate);return <main>{showKeyPrompt&&<div className="keyModal"><div className="keyDialog"><div className="orbMark">◌</div><h2>Connect TimesFM</h2><p>Enter your TSFM.ai API key to enable live Google TimesFM projections. Your key is used only for this session and is never saved.</p><input autoFocus type="password" value={apiKey} onChange={e=>setApiKey(e.target.value)} placeholder="Paste API key"/><button onClick={()=>{setShowKeyPrompt(false);load()}}>Submit key</button><button className="skip" onClick={()=>setShowKeyPrompt(false)}>Continue without key</button></div></div>}<aside><div className="brand"><i/>orbit<span>/</span>forecast</div><small>WORKSPACE</small><a className="active">◈　Overview</a><a>◌　Projections</a><a>⌁　Markets</a><a>▣　Backtests</a><small>MANAGE</small><a>✦　Watchlist</a><a>⚙　Settings</a><footer>●　Data pipeline online</footer></aside><section className="content"><header>Workspace / <b>Overview</b><span className="keybox"><input type="password" value={apiKey} onChange={e=>setApiKey(e.target.value)} placeholder="TSFM API key (not saved)"/><button onClick={load}>Connect</button></span><button>⌘ K　 Search</button><em>A</em></header><div className="hero"><div><label>LIVE MARKET PROJECTIONS · TIMESFM READY</label><h1>See what’s next.</h1><p>Real market data in. Clear projected paths out.</p></div><button className="primary" onClick={load}>↻ Refresh data</button></div><div className="assets">{assets.map(a=><button className={symbol===a[0]?'selected':''} onClick={()=>setSymbol(a[0])} key={a[0]}><strong>{a[0][0]}</strong><b>{a[1]}</b><small>{a[0]}</small></button>)}</div><div className="layout"><article><div className="charthead"><div><h2>{data?.name||symbol} · {symbol}</h2><p>Real-time historical series · dotted line is projected path</p></div><div className="price">{loading?'Loading…':last<10?last.toFixed(4):'$'+last.toLocaleString(undefined,{maximumFractionDigits:2})}<small>● live data</small></div></div>{notice&&<div className="notice">{notice}</div>}<div className="chart">{loading?<div className="loading">Loading market history…</div>:data?.prices?.length?<Chart prices={data.prices} horizon={horizon} forecast={data.forecast}/>:<div className="loading">No data returned</div>}</div><div className="horizons">{horizons.map(h=><button className={horizon===h[0]?'on':''} onClick={()=>setHorizon(h[0])} key={h[0]}>{h[0]}</button>)}</div><div className="rows"><b>{data?.name||symbol} outlook · {horizon}</b><p><span>Projected midpoint</span><strong>{last<10?expected.toFixed(4):'$'+expected.toLocaleString(undefined,{maximumFractionDigits:2})}</strong></p><p><span>Lower scenario · P10</span><strong className="red">{(expected*(1-rate)).toLocaleString(undefined,{maximumFractionDigits:2})}</strong></p><p><span>Upper scenario · P90</span><strong className="green">{(expected*(1+rate)).toLocaleString(undefined,{maximumFractionDigits:2})}</strong></p></div></article>{showSummary&&<aside className="summary"><button className="summaryClose" onClick={()=>setShowSummary(false)} aria-label="Close summary">×</button><h3>Projection summary</h3><div className="signal"><label>MODEL STATUS</label><h2>{loading?'Updating…':'Constructive'}</h2><p>Historical data is loaded. The purple dotted path shows the modeled forward trajectory.</p><div className="bar"><i/></div><small>Research mode · not financial advice</small></div><p><span>Data source</span><b>{symbol==='BTC'||symbol==='ETH'?'CoinGecko':'Yahoo Finance'}</b></p><p><span>Observations</span><b>{data?.prices?.length||'—'}</b></p><p><span>Forecast horizon</span><b>{horizon}</b></p><p><span>Engine</span><b>TimesFM adapter</b></p></aside>}</div></section></main>}
