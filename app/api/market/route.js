import { NextResponse } from 'next/server';
const crypto={BTC:{id:'bitcoin',name:'Bitcoin'},ETH:{id:'ethereum',name:'Ethereum'}};
const cache=globalThis.__orbitMarketCache||new Map(); globalThis.__orbitMarketCache=cache;
export async function GET(req){const u=new URL(req.url),symbol=(u.searchParams.get('symbol')||'BTC').toUpperCase(),requestedHorizon=u.searchParams.get('horizon')||'7d';try{let prices,name=symbol;if(crypto[symbol]){const r=await fetch(`https://api.coingecko.com/api/v3/coins/${crypto[symbol].id}/market_chart?vs_currency=usd&days=30&interval=hourly`,{cache:'no-store'});if(!r.ok)throw Error();const j=await r.json();prices=(j.prices||[]).map(x=>({time:x[0],value:x[1]}));name=crypto[symbol].name}else{const r=await fetch(`https://query1.finance.yahoo.com/v8/finance/chart/${encodeURIComponent(symbol)}?range=1mo&interval=1h`,{cache:'no-store'});if(!r.ok)throw Error();const j=await r.json(),q=j.chart.result[0];prices=(q.timestamp||[]).map((t,i)=>({time:t*1000,value:q.indicators.quote[0].close[i]})).filter(x=>x.value!=null)}if(!prices?.length)throw Error();const result={name,symbol,prices,stale:false,updatedAt:Date.now()};cache.set(cacheKey,result);const cacheKey=`${symbol}:${requestedHorizon}`;const apiKey=req.headers.get('x-tsfm-api-key');if(['5m','15m'].includes(requestedHorizon)){result.providerError='This asset feed is hourly; 5m and 15m forecasts require 5-minute market data.';cache.set(cacheKey,result);return NextResponse.json(result)}if(apiKey&&prices.length){try{const horizonMap={"5m":1,"15m":1,"1h":1,"4h":4,"24h":24,"7d":168,"30d":720,"365d":8760},frequencyMap={"5m":"5min","15m":"15min","1h":"h","4h":"h","24h":"h","7d":"h","30d":"h","365d":"D"};const tf=await fetch('https://api.tsfm.ai/v1/forecast',{method:'POST',headers:{'content-type':'application/json',authorization:`Bearer ${apiKey}`},body:JSON.stringify({model:'google/timesfm-2.5-200m-pytorch',inputs:[{start:new Date(prices[0].time).toISOString(),target:prices.map(x=>[x.value]),metadata:{item_id:symbol}}],parameters:{prediction_length:horizonMap[requestedHorizon]||168,frequency:frequencyMap[requestedHorizon]||'h',quantile_levels:[0.1,0.5,0.9]}}),cache:'no-store'});if(tf.ok){const j=await tf.json();const out=j.outputs?.[0];const point=(out?.mean||j.forecasts||j.forecast||j.point_forecast||[]).map(x=>Array.isArray(x)?x[0]:x);const quantiles={};for(const q of (out?.quantile_predictions||[]))quantiles[q.level]=q.values.map(x=>Array.isArray(x)?x[0]:x);result.forecast={engine:'Google TimesFM 2.5 via TSFM.ai',point,quantiles,timestamps:out?.timestamps||[]};result.engine=result.forecast.engine}else result.providerError=`TSFM provider returned ${tf.status}: ${await tf.text()}`}catch(e){result.providerError=e.message||'TSFM request failed'} }cache.set(cacheKey,result);return NextResponse.json(result)}catch{const old=cache.get(cacheKey);if(old)return NextResponse.json({...old,stale:true});return NextResponse.json({error:'Market data is temporarily unavailable'},{status:502})}}
