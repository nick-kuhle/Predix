// Client-safe forecast ledger helpers. Store forecasts without credentials.
export function createForecastRecord({symbol,assetClass,horizon,frequency,model,provider,inputCutoff,currentPrice,predictedPrice,p10,p50,p90,features}){return{forecastId:crypto.randomUUID(),symbol,assetClass,horizon,frequency,model,provider,inputCutoff,createdAt:new Date().toISOString(),currentPrice,predictedPrice,p10,p50,p90,features,status:'pending'}}
export function settleForecast(record,actualPrice){const error=actualPrice-record.predictedPrice;return{...record,actualPrice,absoluteError:Math.abs(error),signedError:error,directionCorrect:Math.sign(actualPrice-record.currentPrice)===Math.sign(record.predictedPrice-record.currentPrice),status:'settled',settledAt:new Date().toISOString()}}
export function summarizeForecasts(records){const settled=records.filter(x=>x.status==='settled');if(!settled.length)return null;return{count:settled.length,mae:settled.reduce((a,x)=>a+x.absoluteError,0)/settled.length,bias:settled.reduce((a,x)=>a+x.signedError,0)/settled.length,directionalAccuracy:settled.filter(x=>x.directionCorrect).length/settled.length}}
