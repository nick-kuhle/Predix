// Shared free-first market schema and strict forecast validation.
export const HORIZONS={"5m":{frequency:'5min',steps:1},"15m":{frequency:'15min',steps:1},"1h":{frequency:'h',steps:1},"4h":{frequency:'h',steps:4},"24h":{frequency:'h',steps:24},"7d":{frequency:'h',steps:168},"30d":{frequency:'h',steps:720},"365d":{frequency:'D',steps:365}};
export const yahooSymbol=s=>({BTC:'BTC-USD',ETH:'ETH-USD',SPY:'SPY',GLD:'GLD','EURUSD=X':'EURUSD=X',NVDA:'NVDA'}[s]||s);
export function validateSeries(series,frequency){if(!Array.isArray(series)||series.length<4)return {ok:false,error:'At least four observations are required'};const clean=series.filter(x=>Number.isFinite(Number(x.value))&&Number.isFinite(Number(x.time))).sort((a,b)=>a.time-b.time);if(clean.length<4)return {ok:false,error:'Not enough valid observations'};return {ok:true,series:clean,frequency,latest:clean.at(-1)}}
export function validateForecast(forecast,horizon){const spec=HORIZONS[horizon];if(!spec||!forecast?.point?.length)return {ok:false,error:'No valid model forecast returned'};return {ok:true,point:forecast.point.slice(0,spec.steps),quantiles:forecast.quantiles||{},timestamps:forecast.timestamps||[],engine:forecast.engine}}
